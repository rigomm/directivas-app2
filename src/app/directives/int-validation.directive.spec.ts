import { IntValidationDirective } from './int-validation.directive';

describe('IntValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new IntValidationDirective();
    expect(directive).toBeTruthy();
  });
});
